#pragma once

namespace rover12_drivers {

// TODO: checksum(), verify(), <-- use CRC32
//       stuff(), unstuff(), <-- use COBS
//       encode<T>(), decode<T>() <-- top level

} // namespace rover12_drivers
